<%= cloudinary_js_config %>

<%= form_for(application_sponsor) do |f| %>
  <% if application_sponsor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(application_sponsor.errors.count, "error") %> prohibited this application_sponsor from being saved:</h2>

      <ul>
      <% application_sponsor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :icon %>
    <%= f.file_field :icon %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
